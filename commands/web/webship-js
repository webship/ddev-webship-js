#!/bin/sh
#ddev-generated

cd /var/www/html || exit 1

if [ -f yarn.lock ]; then
  yarn && yarn cucumber-js --config cucumber.js "$@"
else
  npm install && npx cucumber-js --config cucumber.js "$@"
fi
