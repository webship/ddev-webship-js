name: ddev-webship-js

# list of files and directories listed that are copied into project .ddev directory
# Each file should contain #ddev-generated so it can be replaced by a later `ddev get`
# if it hasn't been modified by the user.
project_files:
  - web-build/.gitignore
  - web-build/disabled.Dockerfile.webship-js
  - config.webship-js.yml

ddev_version_constraint: '>= v1.24.10'

post_install_actions:
  - |
    # post_install_actions run from inside .ddev/; use .. for project root
    PROJDIR="$(pwd)/.."
    mkdir -p "${PROJDIR}/tests/features" "${PROJDIR}/tests/step-definitions"
    # If package.json exists, add webship-js dependency to it; otherwise create one
    if [ -f "${PROJDIR}/package.json" ]; then
      if ! grep -q '"webship-js"' "${PROJDIR}/package.json"; then
        node -e "
          var pkg = JSON.parse(require('fs').readFileSync('${PROJDIR}/package.json', 'utf8'));
          pkg.devDependencies = pkg.devDependencies || {};
          pkg.devDependencies['webship-js'] = '^2';
          pkg.scripts = pkg.scripts || {};
          pkg.scripts['test:chromium'] = pkg.scripts['test:chromium'] || 'cucumber-js --config cucumber.js';
          pkg.scripts['test:firefox'] = pkg.scripts['test:firefox'] || 'BROWSER=firefox cucumber-js --config cucumber.js';
          pkg.scripts['test:webkit'] = pkg.scripts['test:webkit'] || 'BROWSER=webkit cucumber-js --config cucumber.js';
          require('fs').writeFileSync('${PROJDIR}/package.json', JSON.stringify(pkg, null, 2) + '\n');
        "
        echo "Added webship-js dependency to existing package.json"
      fi
    else
      cat > "${PROJDIR}/package.json" << 'WEBSHIP_EOF'
    {
      "name": "webship-js-tests",
      "version": "2.0.0",
      "description": "Webship-JS Automated Functional Acceptance Testing for DDEV project",
      "type": "commonjs",
      "scripts": {
        "test": "cucumber-js --config cucumber.js",
        "test:firefox": "BROWSER=firefox cucumber-js --config cucumber.js",
        "test:webkit": "BROWSER=webkit cucumber-js --config cucumber.js"
      },
      "devDependencies": {
        "webship-js": "^2"
      }
    }
    WEBSHIP_EOF
      echo "Created package.json"
    fi
    if [ ! -f "${PROJDIR}/cucumber.js" ]; then
      cat > "${PROJDIR}/cucumber.js" << 'WEBSHIP_EOF'
    module.exports = {
      default: {
        timeout: 30000,
        requireModule: ['ts-node/register'],
        require: [
          'node_modules/webship-js/tests/step-definitions/**/*.js',
          'tests/step-definitions/**/*.js',
        ],
        paths: ['tests/features/**/*.feature'],
        format: [
          '@cucumber/pretty-formatter',
        ],
        worldParameters: {
          launchUrl: process.env.LAUNCH_URL || process.env.DDEV_PRIMARY_URL || 'https://localhost',
          minWaitTime: {
            page: 3000,
            before_scenario: 0,
            after_scenario: 0,
            before_step: 0,
            after_step: 0,
          },
        },
      },
    };
    WEBSHIP_EOF
    fi
    if [ ! -f "${PROJDIR}/playwright.config.ts" ]; then
      cat > "${PROJDIR}/playwright.config.ts" << 'WEBSHIP_EOF'
    import type { LaunchOptions, BrowserContextOptions } from 'playwright';

    type BrowserName = 'chromium' | 'firefox' | 'webkit';

    const browser = (process.env.BROWSER || 'chromium') as BrowserName;

    const chromiumArgs: string[] = [
      '--no-sandbox',
      '--disable-dev-shm-usage',
      '--disable-setuid-sandbox',
      '--disable-web-security',
      '--ignore-certificate-errors',
      '--disable-extensions',
      '--incognito',
      '--disable-infobars',
      '--start-maximized',
    ];

    interface PlaywrightConfig {
      browser: BrowserName;
      launchOptions: LaunchOptions;
      contextOptions: BrowserContextOptions;
    }

    const config: PlaywrightConfig = {
      browser,
      launchOptions: {
        headless: true,
        slowMo: 300,
        args: browser === 'chromium' ? chromiumArgs : [],
      },
      contextOptions: {
        viewport: null,
        ignoreHTTPSErrors: true,
      },
    };

    export = config;
    WEBSHIP_EOF
    fi
    if [ ! -f "${PROJDIR}/tsconfig.json" ]; then
      cat > "${PROJDIR}/tsconfig.json" << 'WEBSHIP_EOF'
    {
      "compilerOptions": {
        "target": "ES2020",
        "module": "CommonJS",
        "strict": true,
        "esModuleInterop": true,
        "types": ["node"]
      },
      "include": ["playwright.config.ts"]
    }
    WEBSHIP_EOF
    fi
    if [ ! -f "${PROJDIR}/tests/features/example.feature" ]; then
      cat > "${PROJDIR}/tests/features/example.feature" << 'WEBSHIP_EOF'
    Feature: Example page verification
      As a developer
      I want to verify that the example page loads correctly inside DDEV
      So I can confirm the web environment is working

      Scenario: Verify example page loads and shows expected content
        Given I am on homepage
        Then I should see "home"
    WEBSHIP_EOF
    fi
    if [ ! -f "${PROJDIR}/tests/step-definitions/custom.js" ]; then
      cat > "${PROJDIR}/tests/step-definitions/custom.js" << 'WEBSHIP_EOF'
    const { Given, When, Then } = require('@cucumber/cucumber');

    // Add your custom step definitions here.
    WEBSHIP_EOF
    fi
    cp web-build/disabled.Dockerfile.webship-js web-build/Dockerfile.webship-js
    ddev restart

removal_actions:
  - if grep "#ddev-generated" web-build/Dockerfile.webship-js 2>/dev/null; then rm web-build/Dockerfile.webship-js; fi
